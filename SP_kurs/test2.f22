PROGRAM Flyk22;
BEGIN 
VAR INT16 a, b, x;
SCAN (a);
SCAN (b);
SCAN (x);
IF( (a==b) && (b==x) && (a==x) )
BEGIN
	PRINT (1);
ELSE
	PRINT (0);
END

IF( (a << 0) || (b << 0) || ( x << 0) )
BEGIN
	PRINT (-1);
ELSE
	PRINT (0);
END

IF (!!(a << (b + x)))
BEGIN
	PRINT (10);
ELSE
	PRINT (0);
END

END
